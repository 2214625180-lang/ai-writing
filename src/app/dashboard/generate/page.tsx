import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function GeneratePage() {
  return (
    <PlaceholderPage
      eyebrow="AI Generation"
      title="AI 写作生成模块待接入"
      description="当前已完成受保护路由、页面骨架和服务分层预留。下一阶段可以直接在这里接入模板选择、Prompt 组装和流式输出。"
    />
  );
}
